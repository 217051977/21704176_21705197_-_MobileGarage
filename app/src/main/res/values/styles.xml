<resources>

    <!-- Base application theme. -->
    <style name="appTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_primary_black</item>
        <item name="colorAccent">@color/color_primary_white</item>
    </style>

    <style name="toolbarTheme" parent="appTheme">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorPrimaryDark</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>
    </style>

    <style name="SwitchTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/switchActivated</item>
        <item name="colorSwitchThumbNormal">@color/switchDisabled</item>
    </style>

    <style name="navBarTheme" parent="appTheme">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/lightGrey</item>
    </style>

</resources>
